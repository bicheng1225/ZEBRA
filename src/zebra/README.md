# This folder contains the source code for ZEBRA, including several ablation variants.


step 1. create a environment:
`conda create -n zebra python==3.8.20`

step 2. install package:
`conda activate zebra`
`bash install.sh`

step 3. run example:
`bash run.sh`
